# Quality Score

| Date | Task | Score | Notes |
| --- | --- | --- | --- |
| 2026-05-24 | WebDAV empty file Content-Length fix | 96 | Fixed Gemini High finding in `backend/src/api/webdav/methods.rs`: zero-byte WebDAV `GET`/`HEAD` responses now set `Content-Length` from the computed payload length instead of recomputing `end - start + 1`, avoiding `Content-Length: 1` for empty bodies. Added C-097 and a red-green handler test covering empty-file `GET` and `HEAD`. Verification passed: targeted test first failed with `Content-Length: 1`, then passed after the fix; `DATABASE_URL=postgres://$(whoami)@localhost:5432/file_storage_test_pr32_20260524 cargo test --test handler_webdav_tests -- --nocapture`, `cargo fmt --all -- --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `CODEX_VERIFY_SCOPE=auto bash scripts/codex-cli/tools/pre-push-verify.sh --changed` passed. |
| 2026-05-24 | local Codex auto-fix kickoff prefix safety | 95 | Fixed the gap where local/Desktop Codex publishes could use the exact `🤖 codex auto-fix:` prefix that `gemini-review-kickoff` skips, without running the GitHub Actions state machine that posts the review status table. Local publishes now default to `🤖 codex local auto-fix:`, while the GitHub Actions auto-fix workflow explicitly opts into the workflow-owned prefix with `CODEX_AUTO_FIX_WORKFLOW_OWNS_REVIEW=true`. Added C-096, updated local hook docs and commit constraints, and covered the boundary with red-green tests. Verification passed: targeted red tests first failed, then `cargo test --manifest-path scripts/codex-cli/Cargo.toml --lib commit_and_push_ -- --nocapture`, `cargo test --manifest-path scripts/codex-cli/Cargo.toml --test workflow_state -- --nocapture`. |
| 2026-05-24 | WebDAV/OCR shared verify re-run | 96 | Re-ran PR #32 closeout from the isolated `codex/webdav-fulltext-ocr-plan` worktree after wiring Codex auto-fix and human `git push` to the same verifier. Verification passed with branch-scoped `DATABASE_URL=postgres://$(whoami)@localhost:5432/file_storage_test_pr32_20260524`: `cargo test --test handler_webdav_tests -- --nocapture`, `cargo test --test fulltext_search_tests -- --nocapture`, `cargo test --test handler_files_upload_tests -- --nocapture`, `cargo test --test service_auth_tests -- --nocapture`, `cargo fmt --all -- --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all-features`. Automated curl smoke passed against `http://127.0.0.1:3024/dav`; evidence saved under `docs/evidence/webdav-smoke-20260524-shared-verify/`. The default local `file_storage_test` database is polluted by migrations from another branch, so this closeout uses an isolated DB for reproducible evidence. Residual release-candidate smoke: Finder, iOS Files, rclone, cadaver. |
| 2026-05-23 | Codex shared pre-push verification hook | 95 | Added a single shared verifier at `scripts/codex-cli/tools/pre-push-verify.sh`, rewired `CODEX_AUTO_FIX_VERIFY_COMMANDS` to call it, and added versioned local hook tooling under `scripts/git-hooks/` so human `git push` and Codex auto-fix use the same backend/frontend gates. Added exec-plan and local hook reference docs; updated C-084. Verification passed: red contract tests first failed for missing shared script/hook wiring, then `bash -n scripts/codex-cli/tools/pre-push-verify.sh`, `bash -n scripts/git-hooks/pre-push`, `bash -n scripts/git-hooks/install.sh`, targeted workflow-state contract tests, and `cargo test --manifest-path scripts/codex-cli/Cargo.toml --test workflow_state`. Residual: full backend/frontend gates run when pushing changed backend/frontend code, not during this hook-only change. |
| 2026-05-23 | WebDAV/OCR PR #32 closeout | 96 | Isolated `origin/codex/webdav-fulltext-ocr-plan` worktree closeout only. Fixed WebDAV lock conflict lookup DB errors to fail closed with logs/metric, rejected collection COPY/MOVE into own descendants before side effects, bounded UTF-8 fulltext snippets to 160 chars, and verified OCR disabled/unsupported/missing dependency states do not fail indexing workers. Added C-094/C-095, exec-plan, docs, and curl smoke evidence under `docs/evidence/webdav-smoke-20260523-pr32-closeout/`. Verification passed: `cargo test --test handler_webdav_tests -- --nocapture`, `cargo test --test fulltext_search_tests -- --nocapture`, `cargo test --test handler_files_upload_tests -- --nocapture`, `cargo test --test service_auth_tests -- --nocapture`, `cargo fmt --all -- --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --all-features`, and `scripts/webdav-smoke.sh` against `http://127.0.0.1:3019/dav`. Residual release-candidate smoke: Finder, iOS Files, rclone, cadaver. |
| 2026-05-15 | relaxed review status table comments | 95 | Fixed the relaxed Gemini review clear UX gap: `pr-auto-fix` now persists its JSON result for the state-machine step, and `relaxed_clear` posts a fresh PR comment table with each Gemini finding, Codex status, and solution/explanation instead of only pointing to older comments. Added C-093 and workflow/state script contract coverage. |
| 2026-05-15 | codex auto-fix reused workspace cleanup | 95 | Root caused self-hosted bootstrap failure to stale local edits left in the reused runner workspace before `git checkout -B` switched to the new PR head. The workflow now cleans an existing workspace with `git reset --hard` and `git clean -ffdx` before local mirror/seed hydration, while failing closed if the workspace path accidentally equals `CODEX_LOCAL_REPO_SEED`. Added C-092 and workflow contract coverage. |
| 2026-05-15 | portfolio theme | 96 | 新增第 5 款 Portfolio 主题：theme store 支持 `portfolio` 持久化并应用 `data-theme="portfolio"` + `.dark.portfolio`，ThemeToggle 切换链改为 dark → light → purple → terminal → portfolio → dark，ThemeSection 增加 Portfolio 选项；tokens.css 增加深 slate/blackglass、violet/cyan/gold developer portfolio token block 和 reduced-motion 友好的 CSS-only glow/grid motion。新增 store/toggle/settings/token 红绿测试；focused tests、lint、check:tokens、check:hardcoding、build 通过；Chrome 桌面/移动截图证据保存到 `docs/evidence/portfolio-theme-desktop.png` 和 `docs/evidence/portfolio-theme-mobile.png`。 |
| 2026-05-15 | terminal theme | 96 | 新增第四主题 Terminal：theme store 支持 `terminal` 持久化并应用 `data-theme="terminal"` + `.dark.terminal`，ThemeToggle 切换链改为 dark → light → purple → terminal → dark，ThemeSection 增加 Terminal 选项；tokens.css 增加黑玻璃 cyan/mint terminal token block 与 reduced-motion 友好的轻量 glow/scanline 动效。入口初始化 theme store，让登录页也能读取持久化主题。新增 store/toggle/settings/token 红绿测试，截图证据 `docs/evidence/terminal-theme-login.png`；focused tests、lint、check:tokens、check:hardcoding、build 通过。 |
| 2026-05-15 | settings visual polish | 96 | Settings 页面在不改变已批准布局顺序的前提下增强设计感：`SettingsCard` 增加 group hover、isolate、顶部/侧边 hairline 与图标光感；`settingsUi` 统一输入框、按钮、信息面板的 focus/hover/阴影反馈；Settings Center 顶部面板与 KPI tile 增加细节层次。新增视觉契约回归测试，桌面/移动生产预览截图无错误页、无检测到文本或控件溢出；focused test、lint、build 通过。 |
| 2026-05-15 | webdav permission controls | 96 | API Tokens 中 `Enable WebDAV` 与 `WebDAV read-only` 从浏览器默认 checkbox 升级为可点击权限卡片：原生 input 保留为 `sr-only peer`，视觉层使用自定义圆角勾选框、选中态背景、hover/focus 样式与辅助说明。新增回归测试确认 read-only 切换仍提交 `webdav_read_only: true`；focused test、lint、build、生产预览截图通过。 |
| 2026-05-15 | settings row alignment | 96 | Settings 第一行 Account/Security 改为桌面端等高拉伸，两个表单内容撑满卡片并用 `mt-auto` 将 Save 与 Change password 操作区对齐；第三行 OCR Status/Storage 改为等高拉伸，底部边界齐平。新增回归测试、exec-plan JSON/MD 与生产预览截图证据；focused test、lint、build、DOM 对齐探针通过。 |
| 2026-05-15 | codex auto-fix hermetic agent exec | 95 | Root caused the new codex-cli stall to the local Codex App executor inheriting interactive config and spawning plugin/MCP helpers (`git remote-https https://github.com/openai/plugins.git`, Computer Use MCP) during auto-fix. `codex exec` calls are now normalized with `--ignore-user-config --ignore-rules --ephemeral --model gpt-5.5`, and timed-out agent commands run in their own process group so grandchildren are killed too. |
| 2026-05-15 | settings redesigned layout polish | 96 | Settings 页面从机械三行等高改为更优雅的任务分区：Account/Security 成身份与安全组，WebDAV Access 独占全宽并重排为连接主面板 + 三个说明面板，API Tokens 独占宽区承载创建表单与 token 列表，OCR Status/Storage 保持状态类并排；Account、Security、API Tokens 操作按钮统一收束到底部操作区。新增 focused layout 回归测试与桌面/移动截图证据；聚焦测试、lint、build 通过。 |
| 2026-05-15 | codex auto-fix direct full-file repair | 95 | Relaxed auto-fix now enables `CODEX_AUTO_FIX_DIRECT_FULL_FILE=true`, so BatchFix asks GPT-5.5 for the complete target file and writes it directly instead of first trying patch apply and hitting `context_mismatch`. Legacy patch/retry behavior remains available when the env flag is off; protected files, allowed prefixes, and pre-push validation remain hard boundaries. |
| 2026-05-15 | settings webdav layout cleanup | 95 | Settings 页面移除 Appearance 展示，保留 ThemeSection 独立行为测试；Settings Center 标题栏下方六项在电脑端固定为三行两列：Account/Security、WebDAV Access/API Tokens、OCR Status/Storage，且每行左右卡片等高对齐，移动端保持单列顺序；WebDAV Access 重排为 Connection details、Credential mapping、Setup order、Client notes 四组并在半宽卡片内保持可读。新增红绿 Settings 回归测试、exec-plan JSON/MD 与桌面/移动截图证据；聚焦测试、lint、build 通过。 |
| 2026-05-15 | codex auto-fix relaxed clear mode | 95 | Changed automatic review from strict gatekeeping to relaxed cleanup: codex-fix now clears Gemini review state immediately in relaxed mode, does not request or wait for another Gemini review, marks the loop clean even when pending/push-blocked output remains, and Gemini kickoff no longer fails when Gemini does not answer before the watchdog timeout. |
| 2026-05-14 | codex auto-fix relaxed repair mode | 95 | Relaxed auto-fix now keeps Codex/GPT-5.5 focused on repairing review findings: empty initial patches, context mismatches, empty retry patches, and retry-generation failures continue into full-file fallback; workflow sets `CODEX_AUTO_FIX_STRICT=false`; soft SecurityCheck findings no longer turn successful review fixes into needs-human. Hard safety remains for protected files, checkout correctness, write boundaries, and pre-push validation. |
| 2026-05-14 | webdav fulltext ocr refactor | 95 | Completed the planned moderate refactor: WebDAV route dispatch, auth, method handlers, PROPFIND/XML, locks, path/range/header/XML helpers, and `services::webdav::WebDavService` now have explicit boundaries; fulltext indexing orchestration moved from `task_queue` into `FulltextIndexer`; fulltext snippet/source logic and OCR image/PDF/runtime helpers are split. Added C-089 after fixing shared DB auth-test flakiness found by the final gate. External API/config contracts remain unchanged. |
| 2026-05-14 | ocr runtime pdf status completion | 95 | Closed the remaining OCR deliverables: backend runtime image installs Tesseract/Poppler, Docker Compose wires OCR env, PDF OCR now renders bounded pages through `pdftoppm` before Tesseract, missing dependencies degrade safely, `/api/v1/files/search/ocr/status` reports readiness, and Settings shows OCR/dependency/page-limit status. Added C-087 and focused backend/frontend regression coverage. |
| 2026-05-14 | codex auto-fix bootstrap hardening | 95 | Made self-hosted auto-fix checkout local-first with `CODEX_LOCAL_BARE_MIRROR`, bounded local mirror refresh, mirror/seed hydration before network fallback, a bounded `CODEX_PR_TARBALL_BUDGET_SECONDS` tarball path, explicit bootstrap status/byte diagnostics, and a `Codex checkout/bootstrap blocked` PR comment before fail-closed exit. Added C-086 and workflow contract coverage. |
| 2026-05-14 | fulltext-ocr-deliverable | 94 | 全文搜索收敛为共享 Tantivy index + worker 入队/删除移除/API files 契约；前端搜索框接入 fulltext endpoint 并展示 snippet/source；OCR 增加 `OCR_ENABLED`/`OCR_TESSERACT_BIN` 与缺依赖安全降级。新增 C-085 防止 Tantivy writer LockBusy 回归。 |
| 2026-05-14 | codex auto-fix runner stall closure | 95 | Fixed the remaining local runner stall class by keying workflow concurrency to PR number instead of `github.ref`/actor, adding a `doctor --json` preflight before `pr-auto-fix`, and making doctor warn when `CODEX_AGENT_COMMAND` recursively points to `codex-auto-fix`. Updated C-070, CLI docs, and exec-plan evidence; targeted workflow/doctor regression tests pass. |
| 2026-05-14 | codex auto-fix pre-push validation | 95 | Root caused the latest PR #32 CI failure to an auto-fix generated import of undeclared `react-i18next`. Restored the settings component to existing project patterns and added a pre-push verification hook so frontend auto-fix changes run install, lint, and TypeScript checks before commit/push. |
| 2026-05-14 | codex auto-fix self-hosted frontend validation | 95 | Follow-up from the real self-hosted run: pre-push validation correctly blocked publish, but plain `npm ci` on macOS Node 23 attempted to build `canvas` and then continued into the wrong `npx tsc`. Made frontend verification fail-fast, skip native install scripts, and require local project TypeScript via `npx --no-install`. |
| 2026-05-14 | codex auto-fix push-blocked reporting | 95 | Follow-up from the second real self-hosted run: validation correctly blocked reintroduced `react-i18next`, but the CLI surfaced it as a hard workflow failure. `FeedbackSkill` now converts commit/pre-push/push failures into structured `push_blocked=true` JSON so the review state machine can mark needs-human without publishing a bad commit. |
| 2026-05-14 | codex auto-fix backend format guard | 95 | Follow-up after the successful auto-fix run pushed backend changes through the GitHub API path without triggering a fresh CI run. Local verification caught `cargo fmt --check` and clippy failures in the bot commit; fixed them and added backend fmt/clippy to pre-push validation. |
| 2026-05-14 | codex auto-fix graceful timeout budget | 95 | Root caused PR #32 `codex-fix` failure to the 30 minute step timeout killing `QualityScore` after useful patches had already been applied. Added CLI runtime budgeting, bounded per-call timeout, graceful soft-audit degradation for SecurityCheck/QualityScore failures, workflow budget diagnostics, and regression tests proving partial fixes still return JSON when audits time out or budget is exhausted. |
| 2026-05-14 | codex auto-fix runner cancellation hardening | 95 | 修复本地 Runner 自动修复链路的第三个卡点：workflow concurrency 改为串行排队且不取消 active run，避免 auto-fix 生成并推送补丁后在 Review 状态机阶段被新 review 事件取消；同步 C-070 和 workflow 回归测试。 |
| 2026-05-14 | WebDAV completion closure | 96 | Closed the remaining WebDAV completion gaps by replacing PROPFIND request substring detection with quick-xml parsing/building, hardening Destination handling for reverse-proxy prefixes and source-collection relative paths, adding regression tests for malformed XML and Destination edge cases, and running automated curl smoke against a local backend. Evidence saved under `docs/evidence/webdav-smoke-20260514-codex/`; rclone/cadaver were unavailable locally and Finder/iOS remain manual UI smoke items before release signoff. |
| 2026-05-14 | WebDAV Class 2 follow-up hardening | 95 | Continued WebDAV completion by adding token-bound lock ownership, LOCK refresh behavior, same-token UNLOCK enforcement, recursive Depth infinity PROPFIND, streamed storage-backed COPY, safer DAV href encoding, missing-storage metrics, and service-file test DB serialization discovered during full backend verification. WebDAV integration tests, token tests, frontend tests/build, smoke script syntax, and targeted service file tests pass; external Finder/rclone/cadaver/iOS smoke still requires a running server and credentials. |
| 2026-05-14 | WebDAV Class 2 hardening | 95 | Added WebDAV token scopes, persistent lock enforcement, streaming PUT/GET/Range responses, Depth-aware PROPFIND, `.DS_Store` filtering, soft-delete folder DELETE, file COPY, cache invalidation constraints, and smoke documentation. Targeted WebDAV tests cover lock conflicts, read-only tokens, suffix Range, Depth 0, Finder metadata, folder delete, and COPY. |
| 2026-05-14 | Finder preview 404 orphan filtering | 95 | Root cause traced to active DB rows whose local storage objects were missing, plus macOS Finder AppleDouble `._*` resource-fork files. WebDAV now ignores AppleDouble uploads, SQL/service lists hide historical AppleDouble rows, local list responses filter missing storage objects with warning logs, and the file-list cache key was bumped. Added red-green handler tests for AppleDouble and orphan local records. |
| 2026-05-13 | finder upload preview MIME | 95 | 修复 Finder/文件选择器与 macOS Finder WebDAV 上传的可预览文件落库为 `application/octet-stream` 后无法预览的问题：前端普通上传用 `getUploadMimeType` 包装 FormData 文件类型，后端 multipart/WebDAV PUT 共享扩展名 MIME 兜底；前端预览判断与后端 preview/download 响应也兼容旧 octet-stream 元数据。新增 C-077、exec-plan JSON/MD、前端服务/预览红绿测试与后端 multipart/WebDAV/API preview 红绿测试。 |
| 2026-05-13 | webdav-fulltext-ocr-main-checkout | 95 | Implemented WebDAV v1 and Tantivy fulltext v1 in primary checkout per human correction; OCR remains optional/config gated. |
| 2026-05-12 | codex auto-fix queue timeout guard | 95 | 取消 PR #29 中占用 self-hosted runner 的旧 `codex-fix` run，并为 `Codex Auto Fix (本地 Runner)` 增加 workflow-level concurrency、job/step/agent 三层超时与 queue guard 诊断；新的 Gemini review 会在 runner 分配前取消旧 auto-fix，避免 stale run 队列无限等待；rebase 后修复 remote retry helper 丢 stderr 导致 GitHub API fallback 无法识别网络错误的回归。新增 C-070/C-071 与 workflow_state/repo 契约测试。 |
| 2026-05-12 | auto review ledger module boundary | 95 | 将 `scripts/codex-cli` auto-review ledger 从 `repo.rs`/`skills.rs`/`runtime.rs` 收敛到独立 `review_ledger.rs` 模块：模块统一负责 per-PR/local 路径、完整审计表格、所有 severity 状态、Low/Info tracked 语义、source-fix 判断与 ledger 写入；`repo.rs` 仅保留安全 I/O helper。新增 C-069 与模块红绿测试。 |
| 2026-05-11 | codex-cli symlink write boundary | 95 | 修复 SecurityCheck 指出的 `scripts/codex-cli/src/repo.rs` 符号链接写越界风险：repo/changelog/ledger 写入统一走 no-final-symlink 安全写 helper，Unix 上额外使用 `O_NOFOLLOW`，新增 symlink 指向仓库外文件的红绿回归测试和 C-068 永久约束。 |
| 2026-05-11 | auto review ledger full audit archive | 95 | 将 auto-review ledger 从“已修复项记录”升级为完整 PR review 审计档案：所有 severity（含 Low/Info）都会进入 per-PR/local ledger；无源码修复也写 ledger；每条 issue 记录 suggestion、constraints、auto-fix scope、status、fix method/failure reason、related files 和修复摘要；同时修正只写 ledger 时 JSON `fixed=false`。 |
| 2026-05-12 | upload light empty select refinement | 95 | 修正 light 模式空队列 `Select files` 按钮初版黑绿 graphite 观感过脏的问题：light 专属主 CTA 改为 white/mint/cyan opal 亮面渐变、cyan 细边与深 slate 文案，同时微调 light 下 Cancel、disabled Attach files、URL Upload 的支撑色，保持 dark/purple 不变。更新红绿 CSS 结构测试并重生成 `docs/evidence/upload-empty-buttons-light.png` 视觉证据；focused test、lint、build 通过。 |
| 2026-05-12 | upload empty button palettes | 95 | 重设计上传弹框空队列状态的三主题按钮配色：`Cancel`、disabled `Attach files`、`Select files`、URL `Upload` 拆出语义类，dark 使用 emerald/cyan 主 CTA 与低亮黑玻璃 inactive，light 使用 graphite/mint 主 CTA 与 opal 轮廓，purple 使用紫粉主 CTA 与暗底描边/inactive 层级；新增 C-074、exec-plan JSON/MD、红绿 CSS 结构测试和 `docs/evidence/upload-empty-buttons-dark.png` / `upload-empty-buttons-light.png` / `upload-empty-buttons-purple.png` 视觉证据；focused test、lint、build 通过。 |
| 2026-05-12 | upload purple ready button hover | 95 | 将参考图按钮语言限定应用到 purple 模式上传弹框 footer 的有文件待上传状态：footer 新增 `data-ready-to-upload` 状态标记，`Attach files`/`Start Upload` hover 使用实心紫粉渐变，`Cancel` hover 使用暗底紫色描边，disabled、空队列与其它主题保持原行为。新增 C-073、exec-plan JSON/MD、红绿 CSS 结构测试和 `docs/evidence/upload-purple-ready-attach-hover.png` / `docs/evidence/upload-purple-ready-cancel-hover.png` 视觉证据；focused test、lint、build 通过。 |
| 2026-05-12 | unselected card checkbox contrast | 95 | 强化移动端/卡片未选中勾选框对比：未选中态新增 `card-checkbox-unselected` 外层与 `card-checkbox-unselected-ring` 内圈语义类，外层增加主题化底色、边线、阴影和轻 blur，内圈边线加粗；深色和浅色主题分别新增 unselected surface/border/ring/shadow tokens，避免仅靠透明背景上的细小内圈导致外侧圆环不明显。新增 C-072、exec-plan JSON/MD、红绿回归测试和 `docs/evidence/unselected-checkbox-contrast-mobile.png` 视觉证据；focused test、全量前端测试、lint、token check、build 通过。 |
| 2026-05-12 | mobile selection checkbox visibility | 95 | 修复移动端文件/文件夹卡片左上角未选中勾选框不可见的问题：根因是 `SelectionCheckbox` 对未选中态使用无条件 `invisible group-hover:visible`，触摸设备没有可靠 hover，导致入口消失。改为 `selection-checkbox-hover-reveal` 语义类，默认可见，仅在 `@media (hover: hover) and (pointer: fine)` 的桌面精细指针设备中恢复 hover reveal。新增 C-071、exec-plan JSON/MD、粗指针红绿回归测试和 `docs/evidence/mobile-selection-checkbox-visible.png` 视觉证据；focused tests、全量前端测试、lint、token check、build 通过。 |
| 2026-05-11 | filter dropdown scroll anchoring | 95 | 修复 Files 顶部 Type/Sort 下拉框点击后页面滚动时悬空停留的问题：`DropdownMenu` 不再 `createPortal(document.body)`，移除 viewport `position: fixed` 动态定位，改为在触发卡片内渲染并用 absolute 锚定，因此随 toolbar 一起滚动；打开的 filter card 提升 z-index，避免被相邻 Sort/Type 卡片盖住；同时提升带 `transform: scale(0.75)` 的 toolbar wrapper 层级，避免下拉被后续文件内容遮挡。新增 dropdown 专用更高不透明度 surface token，避免复用透明 filter 卡片背景导致选项过透。新增 C-070、exec-plan JSON/MD、红绿回归测试和 `docs/evidence/filter-dropdown-layering.png` 层级证据；聚焦 filter 测试、全量前端测试、lint、build 通过。 |
| 2026-05-11 | trash card action row | 95 | Trash 卡片 restore/purge 从缩略图浮层移到 meta 信息区下方的独立按钮行；标题、文件说明、倒计时与操作行形成稳定纵向节奏，按钮保持等宽图标操作并继续 stopPropagation，避免误触选择。新增 exec-plan JSON/MD 和 Trash 契约测试，聚焦测试、fluid sizing、lint、build 通过；用本地 Vite + Playwright mock 生成 `docs/evidence/trash-card-action-row.png` 视觉证据。 |
| 2026-05-11 | file drag move hit targets | 95 | 修复桌面端拖文件到文件夹只在图标/缩略图区域 drop 生效的问题：`FolderCard` 的 `dragover/drop` 挂到完整 `data-folder-id` 卡片；修复移动端长按缩略图中心被 hover-only 预览按钮拦截、`pointerup` 释放到源卡片外时不触发移动、以及触摸浏览器原生 `draggable/contextmenu` 抢走长按导致只有震动反馈的问题：粗指针设备禁用原生 HTML draggable，长按 pending 时阻止原生 callout，文件移动长按期间注册全局 `pointerup/pointercancel` fallback。新增 C-069、exec-plan JSON/MD、移动端 smoke 截图和红绿回归测试；相关拖拽测试、全量前端测试、lint、build 通过。 |
| 2026-05-11 | codex-cli implementation documentation | 95 | 新增 `scripts/codex-cli/docs/implementation.md`，按当前源码详细记录 CLI 入口、runtime/pipeline/skill 链路、SEARCH/REPLACE patch 主路径、数据契约、环境变量、输出契约、排障手册、修改代码入口和测试地图；更新 docs README 导航，并补齐本任务 exec-plan JSON/MD。 |
| 2026-05-11 | codex-cli review-text alias | 95 | 将 `codex-auto-fix` 非结构化 Review 输入从 Gemini 专名继续解耦：`pr-auto-fix` 新增通用 `--review-text` 并保留 `--gemini-review` 兼容入口，同传时 fail-fast；`auto-fix-local` 支持 inline `--review-text` 并拒绝与 `--review-file` 同传。新增 C-068、exec-plan 双件和 e2e 红绿测试。 |
| 2026-05-11 | auto review ledger cross-PR archive | 95 | 修复 `docs/auto-review-ledger.md` 只像当前 PR 局部流水账、缺少按 PR 归档的问题：`codex-auto-fix` 现在同时写全局 ledger 与 `docs/auto-review-ledgers/pr-<number>.md` / `local.md`，并新增 C-066、E2E 测试和 PR 路径单测，避免后续 review 记录丢失上下文。 |
| 2026-05-11 | codex-cli doctor install freshness | 95 | 修复本机 `~/.cargo/bin/codex-auto-fix` 停留在 2026-05-03 旧安装版、源码新功能未生效的可诊断性缺口：新增 `codex-auto-fix doctor` / `--json`，报告 PATH、当前二进制、源码新鲜度、`CODEX_AGENT_COMMAND` 与 `git/gh/jq/cargo` 依赖；新增 C-065 永久约束、exec-plan 双件与 doctor 红绿测试。 |
| 2026-05-09 | AFPR-001 exec-plan signoff (方案 A) | 95 | 在 PR #26 `corrupt patch` 失败链根治后补齐 Phase 6 收尾：新增 `docs/constraints/C-057-codex-auto-fix-search-replace-format.md` 永久约束（含历史教训表与失效条件），把 `scripts/codex-cli/docs/exec-plans/auto-fix-patch-reliability.md` 里 P1~P5 全部任务勾选并附完成证据，T6.2/T6.3 标记完成、T6.1 真实黄金集按方案 A 留作 backlog。最终验证 `cargo fmt --all -- --check`、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo test --all`（patch_search_replace 6/6 + 全量）通过；不修改业务代码。 |
| 2026-05-11 | codex auto-fix tarball retry | 95 | 修复 self-hosted Codex Auto-Fix 在 resilient checkout 中遇到 GitHub API tarball `stream ID 1; CANCEL` 后立即 fail-closed 的问题：新增 tarball 下载 helper，`gh api` 与 `curl --http1.1 --retry-all-errors` 双路径校验 tarball 可解压后才继续，保留无法验证 exact PR head 时拒绝运行的安全边界；新增 C-064 与 workflow_state 契约测试。 |
| 2026-05-11 | trash purge shared storage safety | 95 | 针对安全扫描指出的 `purge_expired_trash` 共享 `file_path` 误删风险补强：确认当前实现按 hard-delete 后剩余 DB 引用计数决定是否删存储，不再扣减本次清理数量；新增共享路径回归测试，证明 expired trash 与 active file 共用底层对象时 purge 只删 DB 行、不删存储文件；同步新增 C-063 永久约束。 |
| 2026-05-11 | restore modified migration 034 | 95 | 修复启动失败 `migration 34 was previously applied but has been modified`：恢复已应用的 `034_add_files_deleted_at.sql` 原始内容，把后续 active filename 唯一索引改写移入新增 `035_rewrite_active_file_name_unique_indexes.sql`，并新增 C-062 与 migration governance 红绿测试防止再次回改历史 migration。 |
| 2026-05-11 | codex auto-fix resilient checkout | 95 | 修复 self-hosted runner 在 `actions/checkout` 阶段因 GitHub Git HTTPS `Empty reply from server` / `Failed to connect` 失败、尚未进入 codex-cli 的根因：`codex-auto-fix` 改为本地 seed bootstrap，要求精确 PR head 可验证，缺失 commit 时用 GitHub API tarball 覆盖工作区，并设置 `CODEX_PUBLISH_VIA_GH_API=true` 让后续发布走 GitHub API。新增 C-061 永久约束和 workflow/API 开关红绿测试。 |
| 2026-05-11 | codex-cli remote publish fallback | 95 | 修复 PR Auto-Fix 因 `git push` transient HTTPS 抖动直接失败的问题：保留 `git push` 快路径和 retry；当 3 次均为 transient network error 时，自动改用 GitHub Git Data API 创建 tree/commit 并 non-force 更新当前分支 ref。新增 C-060 永久约束和红绿测试，模拟 `Empty reply from server` 后 API fallback 成功。 |
| 2026-05-10 | codex-cli security remediation loop | 95 | 修复自动闭环缺口：SecurityCheck finding 不再只写 pending，而是在反馈/推送前转成受限 High issue 进入一次安全修复补丁轮，并重跑安全检查；完整文件兜底拒绝 diff/SR 输出，避免把 patch 文本写成源码。同步修复真实安全项：回收站过期清理校验 retention/batch 范围并传播派生资源/存储清理错误，worker metrics 默认绑定 127.0.0.1，显式 `WORKER_HOST=0.0.0.0` 才外露。验证通过 codex-cli fmt/test/clippy、backend fmt/目标测试/clippy。 |
| 2026-05-10 | codex-cli SEARCH/REPLACE six-phase completion | 95 | 补齐 auto-fix patch reliability 6 个 Phase：新增 `Patch`/`RawPatch` 契约、`patch::apply` 统一入口、SR parser/matcher public API、顺序无关与重叠检测、prompt 模块、`CODEX_PATCH_FORMAT` 支持、golden fixtures 5/5、网络错误分类诊断与文档入口。验证通过：`cargo fmt --all -- --check`、`cargo test --all`（86 tests）、`cargo clippy --all-targets --all-features -- -D warnings`、`cargo llvm-cov --all --summary-only`；SR 核心行覆盖率 92.91%，全局既有覆盖率 80.68%。 |
| 2026-05-10 | frontend hardcoding audit | 95 | 新增 `check:hardcoding` 治理脚本与 5 条红绿测试，覆盖 deploy URL、裸 Tailwind 主题色、TSX inline color function、未命名 timing literal，并排除 CSS token 源头、SVG namespace、example URL 与显式 allow。修复运行时代码中的 `localhost:3000` fallback、裸色类、内联 rgba 与 timeout 数字，新增 C-058 和 exec-plan。 |
| 2026-05-10 | frontend full responsive sizing | 95 | 全前端视觉 Tailwind 固定尺寸治理落地：`check:fluid-sizing` 新增 `tailwind-visual` 全局扫描与 filelist/trash、shell/common 分域扫描，排除百分比分数定位等非固定尺寸误报；auth、common dialogs、file grid/list、Trash、upload、preview、Settings、Share 等页面/组件的用户可见尺寸改为显式 `clamp()`/语义响应式值，保持布局、断点、动作顺序与 Trash sticky Console 不变。验证通过：Tailwind visual 扫描、fixed px 扫描、治理脚本测试、全量 Vitest 33 files/158 tests、ESLint、生产 build。 |
| 2026-05-09 | codex-cli SEARCH/REPLACE auto-fix reliability | 95 | BatchFix 主路径从 LLM unified diff 改为 SEARCH/REPLACE block，保留 diff 兼容；新增 SR parser/apply/e2e 覆盖、未修复短路 SecurityCheck/QualityScore/Documentation、`gh pr comment` 与 `git push` transient retry；更新 C-057 与配置/排障/架构文档。 |
| 2026-05-09 | trash mobile cards and batch route verification | 95 | Trash 卡片移动端改为首页同源 3/4/6/8/10 列响应式网格，复用 `SelectionCheckbox`、方形缩略图、首页标题/元信息字号并居中文本，仅保留卡片内还原/彻底删除按钮；Vault Console 保持桌面 content-sized，移动端 summary row 改为像主页搜索框一样填满内部可用宽度，三行 gap 加大到实测 7px，返回上一级/清空回收站行保持右对齐；Trash 页背景改用主页同源 `--filelist-page-bg`，Console 工具栏滚动时 sticky 固定在导航下方，实测滚动 520px 后 top 保持 77px。新增真实 Router batch trash 集成测试；运行态 API 验证批量还原 1 个、批量彻底删除 1 个均 0 failure；Playwright Chrome 生成 desktop/mobile/sticky 证据图。 |
| 2026-05-09 | codex auto-fix malformed direct fallback | 95 | 修复 auto-fix 反复 `补丁应用失败，准备重试` 的剩余根因：`malformed_diff` 不再请求 retry diff，直接进入完整文件兜底；diff preflight 新增 hunk body 行数校验，提前拦截会导致 `git apply corrupt patch` 的结构错误；`git push` 对 `Empty reply from server` 等 transient 网络错误自动重试 3 次。新增 C-056、exec-plan 和红绿回归测试。 |
| 2026-05-09 | gemini kickoff skip precision | 95 | 修复真实 review 闭环验证被误跳过：`gemini-review-kickoff` 原先用 `*codex auto-fix*` 模糊匹配提交信息，导致人工提交 `fix: relax codex auto-fix blockers` 被误判为机器人 auto-fix 提交而跳过 Gemini 请求。现在只跳过精确机器人提交前缀 `🤖 codex auto-fix:`，新增 C-055、exec-plan 和 workflow_state 契约测试。 |
| 2026-05-09 | codex auto-fix warn-only security | 95 | 按最新 review 闭环要求降级 prompt-based SecurityCheck：安全 finding 仍写入 pending explanations/评论，但不再设置 `push_blocked` 或阻止 `--yes` 提交推送；full-file fallback 取消全局 300 行上限，保留 protected files 与 allowed prefixes 两道硬边界，避免 `FileCard.tsx` 这类 451 行真实前端文件的 High priority 修复被挡住。新增 C-054、exec-plan 和红绿 e2e 覆盖。 |
| 2026-05-09 | codex auto-fix diff preflight | 95 | 修复 `codex-auto-fix` 反复输出 `corrupt patch` / `patch fragment without header` 的根因：LLM 生成的 patch 在进入 `git apply` 前新增单文件 unified diff preflight，缺少 `diff --git`、文件头或合法 hunk header 时直接分类为 `malformed_diff` 并进入既有 retry/fallback；补强生成/重试 prompt，并把 SecurityCheck 失败写入 pending explanations。新增 C-053、exec-plan 和红绿回归测试；`scripts/codex-cli` 全量测试通过。 |
| 2026-05-09 | codex auto-fix checkout resilience | 95 | 修复 PR #26 反复红的真实根因：`codex-fix` 在 self-hosted runner 上默认拉 PR merge ref，GitHub 443 瞬断导致 checkout 阶段失败，尚未进入 auto-fix。Workflow 改为先 checkout PR head SHA、`fetch-depth: 0`，再用带 3 次重试的 `gh pr checkout --repo` 对齐分支；新增 C-052 永久约束和 workflow_state 契约测试。 |
| 2026-05-09 | PR26 Gemini review follow-up | 95 | 手动接管 auto-fix malformed patch：修复永久删除先查引用后删记录的竞态，改为删除记录后统一 `cleanup_unreferenced_deleted_files`；新增批量还原/批量彻底删除后端接口，前端 Trash 改用单次批量 API 并报告 partial failure；物理文件和衍生资源清理改为并发 join。补充 service 与前端契约测试；frontend focused tests/lint/typecheck/build、backend fmt/clippy/service_file_tests 单线程通过。 |
| 2026-05-09 | trash summary fit and color selection | 95 | Vault Console summary row 改为 `w-fit`/`max-content`/`max-w-full`，玻璃质感边框长度跟随自身内容收缩；Trash 卡片选中态移除额外 border/ring，仅用 emerald/cyan 背景光晕与 meta 信息条颜色强调。更新 Trash 契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 重新生成 desktop/mobile 证据图并验证 summary row 宽度小于 Console、selected 无额外 border/ring class、有颜色强调。 |
| 2026-05-09 | trash balanced ratio and mobile console rows | 95 | Trash 卡片缩略图从 9:16 改为更协调的 4:5，缩略图边框圆角与最外层 `trashCardFrame` 圆角统一为 `0.24rem`；移动端 Vault Console 改成三行等距结构，summary row 增加玻璃质感边框和背景，批量操作行/基础操作行保持左/右对齐。更新 Trash 契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 验证移动端三行间距均为 3.9px、缩略图比例 1.25、内外圆角一致、summary row 有 1px 玻璃边框。 |
| 2026-05-09 | trash final card and mobile action rows | 95 | Trash Console 移动端动作区重构为两行：选中时批量还原/批量彻底删除同一行左对齐，返回上一级/清空回收站同一行右对齐；未选中时仅基础操作行右对齐，按钮 `w-auto/max-w-max` 自适应文字宽度。Trash 卡片改为完整 `trashCardFrame` / `trashCardThumb` / `trashCardMeta` 三段式 mini vault tile：9:16 主视觉、轻玻璃框、右上圆形 checkbox、紧凑信息带和选中态细光。更新 Trash 契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 验证未选中/选中移动端布局、按钮宽度、9:16 缩略图和 checkbox 位置。 |
| 2026-05-09 | trash 9-16 cards and staggered mobile actions | 95 | Trash 卡片继续精修：checkbox 移到右上角并改为圆形，缩略图固定 9:16 竖图比例，外侧卡片边框改为轻微圆角且覆盖底层 glass-card 默认大圆角；移动端选中后 Console 四个按钮使用 2x2 错位布局，批量还原在左上、批量彻底删除在右下，返回/清空交错排列。更新 Trash 契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 生成 desktop/mobile 证据图并验证缩略图高宽比 1.78、checkbox 右上、圆形和移动错位。 |
| 2026-05-09 | trash card balance and selection clarity | 95 | Trash 卡片比例重新调整为 `aspect-[5/6]`、缩略图约 76%、文字约 24%，卡片与缩略图外框圆角强制为 0；每张卡新增常驻 checkbox，选中后显示勾选、边框与 ring 高亮，批量还原/批量彻底删除在选择后清晰可见；移动端 Vault Console 动作区改成主页搜索工具栏同源紧凑横向布局，按钮尺寸下调。更新 Trash 契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 生成 desktop/mobile 证据图并验证圆角 0、比例、checkbox、批量按钮和 nowrap 动作区。 |
| 2026-05-09 | trash homecard batch operations | 95 | 主页搜索工具栏移除 Trash 按钮，Trash 的 Vault Console 统一到主页 dark 搜索栏工具栏的 `glass-panel glass-panel-toolbar fileListToolbarScale75` 尺寸、按钮样式与配色；Trash 列表项改用主页同源 `glass-card` / `glass-thumb` 卡片语言并保留卡片内还原/彻底删除小按钮；新增点击卡片选择、批量还原、批量彻底删除确认流程。新增 FileListHeader 与 Trash 红绿契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 生成 desktop/mobile Trash 与 Files toolbar 证据图，验证搜索工具栏内 Trash 按钮不存在。 |
| 2026-05-09 | trash toolbar button unification | 95 | Trash 卡片内部右上角倒计时 badge 已删除，仅保留下方倒计时；Vault Console 整条切换为主页 dark 搜索栏工具栏同源 `fileListGlassScope` / `glass-panel glass-panel-toolbar` 风格，返回/清空/还原/彻底删除按钮统一使用主页 `glass-btn` 与 highlight 配色，小图标按钮保留密集网格尺寸。更新 Trash 页面契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 生成 desktop/mobile dark 证据图并验证内部倒计时不存在、底部倒计时存在、Console/按钮 class 正确。 |
| 2026-05-09 | trash upload-tech theme unification | 95 | Trash 三主题统一到 UploadDialog 同源 tech glass 语言：新增 `--trash-tech-*` token 覆盖 grid/beam/scanline/panel/border/corner/shadow，dark 使用黑玻璃 mint/cyan，light 使用 opal graphite/mint，purple 使用紫黑 cyan/magenta。Trash Console 与缩略图增加非交互 grid/beam/scanline/corner overlay，保留 10/5 列、82% 缩略图、标题省略和操作逻辑。新增/更新 token 与页面契约测试；Playwright 生成三主题 desktop/mobile 截图并验证列数、缩略图占比、overlay 存在。 |
| 2026-05-09 | trash three-theme sci-fi system | 95 | Trash 三主题重做为统一 Vault Disposal Console 视觉系统：dark 补齐 Blackglass Reactor 的 `--trash-page-bg`/panel/thumb/countdown token，light 保持 Opal Cyberglass，purple 补齐 Nebula Vault 专属 page/panel/thumb/countdown/action token，三套不再依赖默认继承。新增 `trashThemeTokens.test.ts` 覆盖 dark/light/purple 三主题；聚焦测试、lint、typecheck、build 通过；Playwright 生成三主题桌面/移动截图并验证 desktop 10 列、mobile 5 列、三主题背景非空。 |
| 2026-05-09 | trash opal mint theme | 95 | Trash light 模式从旧灰蓝改为 Opal Black / Electric Mint：页面背景切到珍珠白、柔和石墨与薄荷/珊瑚光晕，Console 改为轻薄 opal 仪表带，缩略图保持黑曜石质感并加入 mint/coral 微光，普通倒计时和还原按钮改为 electric mint，危险态改为 coral。更新 light token 契约防止回退；Trash 页面测试、light token 测试、lint、typecheck、build 通过；Playwright 重新生成桌面/移动 light 截图并验证 `data-theme=light`、桌面 10 列、手机 5 列。 |
| 2026-05-09 | trash light contrast redesign | 95 | Trash 页 light 模式改为专属冷钢灰蓝色板：新增 `--trash-page-bg` 并通过 PageLayout 挂到整页背景，Console/缩略图/占位符/倒计时/按钮均覆盖 light 专属高对比 token，避免继承过亮通用背景。新增 light token 回归测试和 Trash 页面背景契约测试；聚焦测试、lint、typecheck、build 通过；Playwright 生成桌面/移动 light 截图并验证 `data-theme=light`、桌面 10 列、手机 5 列。 |
| 2026-05-09 | trash compact gallery polish | 95 | 回收站卡片按参考图继续收紧：卡片/缩略图/Console 改为极小圆角，缩略图保持 82% 主视觉占比，操作按钮缩小为图标级，倒计时改为更细小且按剩余天数使用主题化强调色；Vault Console 从大面板压缩为单行控制台，移动端按钮高度同步收紧。Trash 页面测试、lint、typecheck、build 通过；Playwright 重新生成桌面深色与移动紫色截图，验证桌面 10 列、手机 5 列、标题单行省略、圆角约 2.5px/1.75px。 |
| 2026-05-09 | trash dense thumbnail cards | 95 | 回收站卡片改为缩略图优先密集网格：手机 5 列、平板/桌面 10 列，标题单行省略，缩略图区域 80%+，非图片显示大占位符；修复 Trash 自身打开时覆盖返回来源的问题，返回优先回到最近 `/files?...`。缩略图接口允许当前 owner 的 deleted 图片生成/读取 thumbnail，但不放开 download/preview。前端 lint/type/test/build、后端 fmt/clippy/定向测试通过；Playwright 验证 10/10/5 列、`ellipsis/nowrap`、缩略图比例约 0.8、主题变量随 light/dark/purple 变化。 |
| 2026-05-09 | trash vault card redesign | 95 | 回收站从列表改为响应式卡片网格，新增 `article` 语义回归测试；顶部控制区与文件卡片统一为科技 Vault 视觉，保留英文 Trash 标题、返回上一级、清空、还原和彻底删除操作。前端 lint/type/focused tests/build 通过；Playwright 桌面与移动验证 2 张卡片、0 个 table、页面错误为 0，并生成截图证据。 |
| 2026-05-09 | trash UX and drag auto scroll | 95 | 修复回收站返回硬编码、页面视觉粗糙、运行态旧后端导致删除后看不到回收站文件的问题；重启后端并确认 `deleted_at` migration 已应用。React Query Devtools 回退官方稳定浮层；新增桌面/移动拖拽边缘自动滚动工具与红绿测试。前端 lint/type/test/build 和后端回收站聚焦测试通过。 |
| 2026-05-08 | trash soft delete | 95 | 文件删除改为 30 天回收站软删除：新增 `deleted_at`、active-only 同名唯一索引、回收站 API/页面、还原/彻底删除/清空和 `trash_cleanup` 后台任务；补齐后端服务与前端页面红绿测试。 |
| 2026-05-08 | auto review comment checklist e2e | 95 | 补齐全自动 review 评论区验证缺口：新增 `pr-auto-fix` e2e，通过 fake `gh pr comment` 捕获自动生成的 PR 评论正文，断言包含 `Medium/Medium+/High/Critical 对应状态` 表、已解决/未解决标记和对应 issue 文案；新增 C-051 与 exec-plan 双件。 |
| 2026-05-08 | auto review local ledger | 95 | 修复 Gemini Review 问题解决后缺少本地可追溯记录的设计缺口：`codex-auto-fix` 在提交/反馈前写入 `docs/auto-review-ledger.md`，逐条记录 Gemini 问题、状态、解决答案/未解决原因和修改文件；stdout JSON 新增 `review_record_path`。新增 C-050、exec-plan 双件和 e2e 红绿测试。 |
| 2026-05-08 | auto review status list comments | 95 | 修复全自动 review 输出层缺口：状态机所有 clean/pending/blocked/round-max 评论都会指向上方 `Medium/Medium+/High/Critical 对应状态` 表；Gemini watchdog quota/timeout 评论明确说明没有新的 Gemini Review 输入，因此无法生成新的问题清单。新增红绿测试覆盖 workflow state 与 watchdog 文案，避免再靠人工补发清单。 |
| 2026-05-08 | codex-cli priority severity closure | 95 | 全自动 review 明确覆盖 `medium priority`、`medium+ priority`、`high priority`、`critical priority` 四档写法：severity 归一化剥离 `priority` 后缀，inline badge alt 文本同样支持 `high priority`，DecisionSkill 会选择四档进入自动修复，PR 评论表头改为 `Medium/Medium+/High/Critical 对应状态`。 |
| 2026-05-08 | codex-cli one-to-one review status | 95 | 将 PR #24 的人工 Medium/Medium+ 对应表固化进通用 codex-cli：stdout JSON 新增 `issue_statuses`，PR 评论默认渲染一一对应状态表，每个 Gemini issue 一一标记 resolved/pending/blocked；workflow needs-human 提示改为指向一一对应状态。新增 C-049 并扩展 C-046，红绿测试覆盖 JSON 与评论块。 |
| 2026-05-07 | review fallback cleanup | 95 | 继续清理 PR #24 最新 Gemini 反馈：Settings Back 优先用 router-managed history index 判断是否有应用内上一页，直接打开 `/settings` 时 `replace` 到 `/files`，避免把用户带出应用；拖拽移动把 root/absent `""` sentinel 显式归一，source payload 不再靠 truthy filter 且不会把 `""` 传给 `moveFolders`，拖动未选中项不会清空已有 selection；Grouped/Virtual file-list 的 FileGrid drop/preview/share adapters 改为稳定引用，`handleDropOnFolder` 用 ref 读取最新 selection 以保持 callback 稳定；review-to-json 支持 `file:line: summary` inline heading。新增 C-048，扩展 C-036/C-043/C-044/C-045，目标回归测试通过。 |
| 2026-05-07 | auto review transparency and remaining Medium cleanup | 95 | 修复 PR #24 最新 Gemini Medium/High：移动端拖拽释放时使用 pointer hit stack 查找被拖拽卡片遮住的 folder target，FileCard/FolderCard 共用 helper；批量移动 self-drop guard 改为只从 folder move payload 排除目标 folder，不再阻断同批 selected files 移入目标。codex-cli 评论与 JSON 新增 issue 级 `fixed_explanations`，PR 评论分区展示“已自动修复问题/未自动修复问题”，避免只给笼统 pending。新增 C-046/C-047 和红绿测试。 |
| 2026-05-07 | PR #24 review cleanup | 95 | 修复 auto-review 误报 clean 的根因：`review-to-json` 现在解析 Gemini inline `![medium]`/`![high]` badge comments，并覆盖无正文 badge finding，不再把 actionable inline findings 转成 0 issues。同步处理 PR #24 Medium/High：移动拖拽态加 `pointer-events-none`、新 pointer interaction 重置 preview suppression、breadcrumb/drop 统一扩展已选批量项、部分移动失败后刷新、`FileCard` memo callback 防 stale、grid/virtual-grid card handlers 稳定化、根目录 sentinel 归一为 `null`。新增红绿测试和 C-044/C-045；验证包含 codex-cli、前端全量 test/lint/build。 |
| 2026-05-07 | settings quick nav home icon | 95 | Settings 删除 Quick nav/sidebar 与所有 `Jump to ...` hash 链接；标题左侧 `Settings2` 图标改为可访问按钮 `Go to files home` 并导航 `/files`，原 `Back` 继续 `navigate(-1)` 保留上一级历史。新增红绿 routed regressions；聚焦测试先红后绿，lint、全量前端测试、build 通过，build 仅保留既有大 chunk 警告；补充 Settings 截图证明 Quick nav 不存在。 |
| 2026-05-07 | settings back history | 95 | Settings header action 从硬编码 `navigate(\"/files\")` / `Back to Home` 改为历史 `navigate(-1)` / `Back`，保留来源页面 query（例如 `/files?folder=folder-1`）。新增 Settings routed regression，复用 C-036 约束；聚焦测试、lint、全量前端测试、build 通过，build 仅保留既有大 chunk 警告。 |
| 2026-05-07 | batch drag move | 95 | 多选文件/文件夹后，拖动任意已选文件或文件夹到目标文件夹会扩展为当前全部选中项；拖动未选项保持单项移动。文件卡片新增移动端长按拖拽并抑制长按后的预览点击，普通/虚拟/混排/分组网格均接入移动端 file drop；补充 C-043、exec-plan 双件、移动视口截图和红绿测试。聚焦测试、相关回归、lint、全量前端测试、build 通过，build 仅保留既有大 chunk 警告。 |
| 2026-05-07 | folder drag move | 95 | 文件夹卡片支持移动端长按拖拽，普通短按不触发移动；FolderGrid/MixedGrid/VirtualizedMixedGrid 均把移动端 drop 接到统一动作层。修复 FileList 文件夹 drop adapter 空实现，`useFileActions` 统一处理文件/文件夹拖到目标文件夹和面包屑，拖到自身 no-op。新增红绿测试、C-042、exec-plan JSON/MD 与移动视口截图；聚焦测试、相关回归、全量前端测试、lint、build 通过，build 仅保留既有大 chunk 警告。 |
| 2026-05-07 | medium review visible scope | 95 | 明确 Gemini/Codex 自动 Review 闭环中 plain `Medium` 与字面量 `Medium+` 一样是 actionable：新增 workflow-state 回归测试，状态脚本 label/comment 统一写 `Medium/Medium+`，更新 C-027/C-035 与 codex-cli reference docs，避免人类和自动化误读为只处理 `Medium+`。 |
| 2026-05-07 | parallel chunked upload worker pool | 96 | 分片上传调度从固定批次 barrier 改为有界 worker pool，默认每文件 4 路并行；任一 lane 完成后立即补下一个 pending part，并用完成集合避免并发进度重复计数。新增红绿测试覆盖即时补位，新增 C-041 和 exec-plan 双件；目标测试、lint、全量前端测试、build 通过，build 仅保留既有大 chunk 提示。 |
| 2026-05-06 | auto-fix weekly failure report and reviewer template | 95 | 新增 `codex-auto-fix auto-fix-weekly-report`，从自动修复 JSON/JSONL 聚合 `apply_fail_reason`、文件路径、`fallback_used`、`final_status` Top 5；Review 指南固化自动修复友好模板；新增 C-040，明确没有真实失败样本前不得默认增加 `git apply --check`。 |
| 2026-05-06 | codex-cli review JSON shell wrapper and PR handoff | 95 | 补齐 `scripts/codex-cli/tools/review_to_json.sh` 兼容入口，委托给 `codex-auto-fix review-to-json`，避免第二套 parser；新增红绿测试验证 wrapper 输出与 CLI 输出一致，新增 C-039 永久约束，并准备随 review JSON 主输入改动一起提交 PR。 |
| 2026-05-06 | codex-cli review JSON primary input and auto-fix hardening | 95 | 将 `review-to-json` 产物升级为默认 `pr-auto-fix --review-json` 主输入，并保留 `USE_REVIEW_JSON=false` Markdown 回滚；`StructuredReview` 转为 `ReviewData` 时保留 constraints，`ReadReviewSkill` 复用预解析数据。`git apply` 失败现在分类并把真实 stderr、最新源码、max hunks/changed-lines 预算注入重试 prompt；full-file fallback 增加允许前缀、保护文件、300 行默认上限；stdout 增加 `apply_fail_reason`、`retry_count`、`fallback_used`、`final_status`。新增 C-038、e2e/workflow/review JSON 覆盖。 |
| 2026-05-06 | /files route refresh scroll restoration | 95 | 移除 `ScrollRestoration.scopeFor()` 中对 `/files` 的路径排除，让主文件浏览页与 `/settings` 共享 history-entry + URL fallback 持久化；`restore()` 对非零目标改为帧驱动重试循环（≤120 帧≈2s、容差 ±2px），并监听 `wheel`/`touchmove`/`keydown` 一次触发即放弃，避免与异步列表/用户手动滚动竞争。新增两条 `/files` 刷新红绿测试（同 history key 与 key 变化），扩展 C-036 禁止路径白/黑名单并强制重试+用户输入退出策略，新增 exec-plan 双件。`vitest` 6/6、`tsc -b`、`eslint` 通过。 |
| 2026-05-06 | extend virtual list frame budget | 95 | 修复进入文件夹刷新后滚动位置丢失：延长 `RESTORE_MAX_ATTEMPTS` 从 60 到 120（约 2 秒），给虚拟列表挂载和 `loadMore` 补页足够时间。`tsc -b`、`eslint` 通过。 |
| 2026-05-06 | refresh deep file-list scroll | 95 | 修复刷新文件列表深位置不生效：识别刷新后 infinite query 可能只加载第一页，保存位置超过当前 document 高度时浏览器会夹住 `scrollTo`；恢复逻辑改为高度不够且 `hasMore` 时先 `loadMore`，列表变长后继续重试，达到高度或无更多页时再标记已恢复。新增深位置刷新补页与多轮补页回归测试；导航聚焦测试、lint、全量 test、build 通过。 |
| 2026-05-06 | refresh scroll restoration | 95 | 修复浏览器刷新后丢失浏览位置：普通路由同时保存 history-entry 与 URL fallback，并只在非 PUSH 场景读取 URL fallback，避免新进入页面继承旧滚动；文件列表增加 `beforeunload` 保存。新增 route key 变化刷新与 file-list 刷新回归测试，更新 C-036 与 exec-plan；导航聚焦测试、lint、全量 test、build 通过。 |
| 2026-05-06 | plain sort virtual scroll anchor | 95 | 针对 plain sort 返回位置漂移：确认 `type/time` 走 grouped view，其他排序走 virtualized mixed/file grid；为虚拟列表容器、行、spacer 添加 scoped `overflow-anchor: none`，避免浏览器 scroll anchoring 在 spacer 高度变化时自动改 scrollY。新增虚拟网格稳定类回归测试，更新 C-036 与 exec-plan；聚焦测试、lint、全量 test、build 通过。 |
| 2026-05-06 | settings return folder scroll | 95 | 修复次级文件夹进入 Settings 再 Back 后恢复到固定位置的问题：Settings 导航前同步保存当前 file-list scroll，文件列表跳过这次 route-transition cleanup 覆盖。新增 folder -> Settings -> Back 红绿回归测试，更新 C-036 与 exec-plan；聚焦测试、lint、全量 test、build 通过。 |
| 2026-05-06 | settings navigation simplification | 95 | 删除 Settings Quick nav 与同页 hash 导航，页面只保留历史 Back；文件/文件夹进入下级与返回上级继续走 URL/history 和既有滚动恢复。新增 Quick nav absence 红绿回归测试，更新 C-036 与 exec-plan；聚焦测试、lint、全量 test、build 通过。 |
| 2026-05-05 | three theme backgrounds | 95 | Dark/light/purple 三套主题背景升级为多层主题化渐变；文件列表从 `background-color` token 改为 `background-image` token 以渲染酷炫背景。新增 token 回归测试、exec-plan JSON/MD 与三主题截图证据；目标 test、layout token check、fluid sizing、lint、diff check、build 通过，build 仅保留既有大 chunk 提示。 |
| 2026-05-05 | navigation scroll restoration | 95 | Settings 页按钮从 `Back to Home` 改为历史 `Back`；Quick nav 锚点用 replace 不污染 Back 历史；route-level scroll restoration 新路由无记录时回顶部，Back/刷新按 history entry 恢复；文件列表继续按 folder/sort/mime/search 保存位置。新增红绿回归测试与 C-036 永久约束，目标测试、lint、全量 test、build 通过。 |
| 2026-05-05 | navigation scroll restoration | 95 | Settings 页按钮从 `Back to Home` 改为历史 `Back`，保留来源文件夹 query；文件列表按 folder/sort/mime/search 持续保存滚动位置，并在进入文件夹、浏览器 Back、刷新/remount 时有记录则恢复、无记录才回顶部。新增红绿回归测试与 C-036 永久约束，目标测试、lint、全量 test、build 通过。 |
| 2026-05-05 | purple nebula bloom theme | 95 | Purple 模式重设计为梦幻宇宙星云风格：page/nav/footer/filelist/upload/dialog/settings token 使用深紫、fuchsia、rose 与 cyan 星光层，保留 DOM/布局/尺寸不变；新增 purple token 红绿回归测试，token/layout/fluid/lint/full test/build 通过，并生成 `/files` 与 `/settings` 桌面/移动截图证据。 |
| 2026-05-05 | codex-cli medium plus severity | 95 | 修复 Gemini Review 字面量 `Medium+` 未进入自动修复/pending 的漏洞：severity 判定集中到 `types.rs`，默认 allow-list 扩展为 `Critical,High,Medium+,Medium`，`CODEX_ALLOWED_SEVERITIES=Medium` 仍覆盖 `Medium+`；新增红绿回归测试、C-035 约束与使用文档。 |
| 2026-05-05 | light tech chrome nav footer | 95 | Light 模式顶部标题栏与底部展示栏改为深色高级科技 chrome：nav/footer token 使用 slate/cyan/purple 发光层，macOS titlebar light override 不再回退白色，NavBar 改用 background shorthand 正确渲染渐变；补充 token 回归测试、桌面/移动截图证据，目标测试、lint、build 与布局治理通过。 |
| 2026-05-05 | frontend fluid sizing PR5 global | 95 | 将 `check:fluid-sizing` 扩展 `--scope=global` 和 signed px 检测；全局 tokens、CTA/nav shadow、devtools min-height、preview text calc 与 hover translate 固定视觉尺寸收敛为 `rem`，observer/clipboard 非视觉例外显式标注；补充 in-app browser `/files` 截图证明。 |
| 2026-05-05 | frontend fluid sizing PR4 dialogs | 95 | 将 `check:fluid-sizing` 扩展 `--scope=dialogs` 并补红绿测试；确认弹窗/文件夹弹窗 shadow、grid、blur 与小字号固定视觉尺寸改为 `clamp()`/`rem` 语义变量；补充 in-app browser 新建文件夹弹窗截图证明。 |
| 2026-05-05 | frontend fluid sizing PR3 preview | 95 | 将 `check:fluid-sizing` 扩展 `--scope=preview` 并补红绿测试；预览舞台、PDF loading、scanline、neon shadow、thumbnail sizes 固定视觉尺寸改为 `clamp()`/`rem`，保留 observer rootMargin 的显式非视觉例外；补充 in-app browser 预览截图证明。 |
| 2026-05-05 | frontend fluid sizing PR2 filelist | 95 | 将 `check:fluid-sizing` 扩展 `--scope=filelist` 并补红绿测试；文件列表/网格域固定视觉尺寸改为 `clamp()`/`rem`，保留 IntersectionObserver rootMargin 的显式非视觉例外；补充 in-app browser `/files` 截图证明。 |
| 2026-05-05 | frontend fluid sizing PR1 | 95 | 新增 `check:fluid-sizing` 尺寸治理脚本与红绿测试，PR1 范围内 common/layout/upload/shared 固定视觉尺寸改为 `clamp()`/`rem`/语义变量；上传弹窗契约测试覆盖 dynamic viewport、safe-area、动态网格与模糊 token；补充 in-app browser 截图证明并新增 C-030 永久约束。 |
| 2026-05-05 | upload dialog mobile spacing | 96 | 修复上传弹窗移动端贴边：backdrop 改用 `100dvh`、safe-area 四边 padding，surface 最大高宽按留白扣减；移除冲突静态 `vh/max-w` Tailwind 类。新增 CSS 契约测试、C-028 永久约束、移动视口截图，上传相关测试/lint/build 通过。 |
| 2026-05-05 | auto review loop state machine | 96 | 将 Gemini/Codex 自动 Review 闭环升级为显式状态机：CLI 输出 pending/clean 字段，workflow 通过测试脚本区分 clean/pending/blocked/round-max；pending 不再误报无需修复，auto-fix commit 触发 CI，kickoff 跳过 auto-fix commit 避免重复 Gemini 请求。 |
| 2026-05-04 | PR #12 backend CI fix | 95 | 修复 Backend (Rust) CI 的 `clippy::items_after_test_module`：将 `storage.rs` 的测试模块移动到生产 impl 之后，新增 C-026 永久约束；本地 fmt/clippy/定向测试通过。 |
| 2026-05-04 | upload review follow-up | 95 | 修复 PR 遗留反馈：前端全文件与 worker SHA-256 改为分块增量读取，避免大文件整块 `arrayBuffer()`；S3 `copy_object` copy source 对对象 key 做百分号编码并保留 `/`；codex-cli 在 `git apply` 失败后带最新源码重试一次。新增回归测试与 C-025/ai-auto-fix 永久约束，定向前后端与 codex-cli 测试通过。 |
| 2026-05-04 | settings page optimization | 95 | Settings 页面保守视觉与体验优化：新增 settings-local UI helper，统一表单/错误/按钮语义样式，改善移动端 email 与 API Token 标题换行，快速导航改为数据驱动；补充 Settings 回归测试并完成 test/lint/token/build/桌面+移动视觉检查。 |
| 2026-05-04 | chunked upload CORS header fix | 95 | 修复浏览器分片上传预检失败：后端 CORS 允许 `X-Part-SHA256`，新增预检回归测试和 C-024 永久约束。 |
| 2026-05-04 | upload completeness hardening | 96 | P0-P3 上传链路补强：folder ownership 校验覆盖普通/秒传/分片完成，上传 handler DB 测试用 `serial_test` 隔离；前端实现可恢复分片会话与每片 SHA，后端校验分片 SHA 和大小；秒传跨用户复制改存储层 copy，分片完成打 metrics；删除未使用 `useFileUpload`，测试迁到实际 UploadDialog controller。目标前后端测试通过。 |
| 2026-05-04 | themed tech dialogs | 94 | 文件操作 glass 弹窗统一升级为主题化科技视觉：`ConfirmDialog` 覆盖新建/批量移动/批量删除/批量分享/重命名/删除，`Modal` 覆盖单文件分享；新增行为不变回归测试，前端 lint/test/build 通过。 |
| 2026-05-04 | codex-cli toolbox extraction design | 94 | 整理 `scripts/codex-cli` 独立工具箱改造设计，明确当前可复用能力、Gemini/GitHub/仓库规则耦合点、配置文件方案、P0-P3 改造清单、迁移步骤与验收标准。 |
| 2026-05-03 | image preview pan after zoom | 95 | 图片预览放大后支持 pointer 拖动平移，缩回 1x、切换文件和 Reset 会清除 pan；新增 `useImagePan` 与 `ImagePreview` 回归测试，并记录 C-019 永久约束。 |
| 2026-05-03 | frontend upload logic completion | 96 | 审查并补齐上传取消与 URL 上传边界：运行中队列取消会 settle，旧 `useFileUpload` hook 传递并中止 `AbortSignal`，上传弹窗卸载时统一取消；URL 上传在读取 body 前按 `Content-Length` 拦截超限文件并支持 `Content-Disposition` 文件名。新增 C-017/C-018 约束，前端 test/lint/build 通过。 |
| 2026-05-03 | reviewer cleanup after upload hardening | 95 | 收敛 reviewer 后续意见：上传 drag/drop handler 使用稳定引用，SHA worker cleanup 简化并避免重复 settle，codex-cli async 路径改用 `tokio::fs`，新增 C-016 约束。 |
| 2026-05-03 | frontend upload logic hardening | 95 | 上传取消从 UI-only 升级为 `AbortSignal` 全链路中止，分片失败/取消后清理服务端会话；补充 MIME 扩展名兜底与危险扩展名阻断，新增 C-015 约束与回归测试。 |
| 2026-05-03 | Gemini medium findings cleanup | 94 | 修复最新 Gemini Review 的 3 个 Medium：`useThrottle` leading-edge 行为、`run_local_codex_command` 临时 prompt 文件错误路径清理、UTF-8 截断效率；补充 C-013/C-014 约束。 |
| 2026-05-03 | Gemini review trigger integration test | 91 | 真实 PR 测试确认 `gemini-kickoff` 可跑，但 `codex-fix` 只监听 issue_comment 会漏掉 Gemini 的 pull_request_review。workflow 改为同时监听 review submission，并拼接 inline comments；新增 C-012 约束。 |
| 2026-05-03 | PR review assertion and filename hardening | 94 | 修复 `LocalStorage::local_filename` 多字节 UTF-8 兜底截断按字符计数的问题，新增多字节扩展名回归测试；清理 backend 测试中的裸 `matches!` 假断言并新增 C-011 永久约束。 |
| 2026-05-03 | backend coverage gate baseline correction | 90 | 真实 `cargo llvm-cov` 全量测试通过，但全局行覆盖率为 23.08%，原 90% CI 门槛是未来目标误作当前硬门槛。CI 改为 23% 基线守门并同步约束文档，后续只能随补测上调。 |
| 2026-05-03 | frontend large component split completion | 95 | 完成 `UploadDialog.tsx`、`FilePreviewContent.tsx`、`MarkdownPreview.tsx` 拆分，目标大文件均降至阈值内；修复 `useThrottle` React Compiler lint 问题。`test`、`typecheck`、`lint`、`build` 通过，仍有既有 Vite chunk 循环与 vendor 大包警告。 |
| 2026-05-03 | codex-cli auto review loop hardening | 93 | 补齐多轮 `max_rounds` 闭环、严格 JSON 安全审计、质量评分可用性、`gpt-5.4` 默认模型、结构化修复尝试统计与 medium+ 未修复说明策略。`scripts/codex-cli` 单测通过。 |
| 2026-05-01 | CI pipeline hardening (#6) | 94 | `.github/workflows/ci.yml` 新增 coverage(llvm-cov, ≥90%)、cargo-audit(阻塞)、cargo-outdated(非阻塞)、`tsc -b --noEmit`、bundle-size budget(200KB/400KB gzip) 与 `cargo doc -D warnings`。新增 `frontend/scripts/check-bundle-size.mjs` 和 `docs/constraints/ci-pipeline.md` 形成永久约束。 |
| 2026-05-01 | Preview feature bug fixes | 96 | 修复图片预览放大缩小旋转重置功能失效和视频预览循环播放功能失效。使用 CSS 变量 + Tailwind 任意值语法实现图片变换，添加 useEffect 确保视频 loop 属性正确同步。构建验证通过。 |
| 2026-04-30 | frontend component split (second round) | 95 | 完成 `UploadDialog.tsx`、`FilePreviewContent.tsx`、`FileListContent.tsx` 的第一轮组件拆分。新增 6 个独立组件，`UploadDialog.tsx` 减少 39% 代码。构建、类型、lint 全通过。 |
| 2026-04-30 | frontend file service split | 91 | 服务层完成领域拆分并保留兼容 facade；`FileList.tsx` 降至 300 行内。剩余大文件：`FileListContent.tsx`、`UploadDialog.tsx`、`FilePreviewContent.tsx`、`MarkdownPreview.tsx`。 |
| 2026-04-27 | backend error boundary unification | 92 | 建立了 `auth` / `file chunked upload` 领域错误边界，待继续扩展到其他 services。 |
